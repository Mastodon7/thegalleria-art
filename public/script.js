const slides=[...document.querySelectorAll('.slide')],dots=document.getElementById('dots'),counter=document.getElementById('counter'),auto=document.getElementById('auto');let current=0,timer=null;slides.forEach((s,i)=>{const b=document.createElement('button');b.textContent=i+1;b.title=s.dataset.title;b.onclick=()=>show(i);dots.appendChild(b)});function show(i){current=(i+slides.length)%slides.length;slides.forEach((s,n)=>s.classList.toggle('active',n===current));[...dots.children].forEach((b,n)=>b.classList.toggle('active',n===current));counter.textContent=`${current+1} / ${slides.length}`;}document.getElementById('prev').onclick=()=>show(current-1);document.getElementById('next').onclick=()=>show(current+1);auto.onclick=()=>{if(timer){clearInterval(timer);timer=null;auto.textContent='Auto';auto.setAttribute('aria-pressed','false')}else{timer=setInterval(()=>show(current+1),7000);auto.textContent='Pause';auto.setAttribute('aria-pressed','true')}};document.addEventListener('keydown',e=>{if(e.key==='ArrowRight')show(current+1);if(e.key==='ArrowLeft')show(current-1)});show(0);